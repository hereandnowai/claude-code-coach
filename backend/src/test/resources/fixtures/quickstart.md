# Quickstart

This quickstart guide will have you using Claude Code in a few minutes.

## Step 1: Install Claude Code

To install Claude Code, use the native installer:

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

## Step 2: Log in to your account

Start an interactive session with the `claude` command and follow the prompts.
