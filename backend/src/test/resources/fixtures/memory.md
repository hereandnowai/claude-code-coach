# How Claude remembers your project

## CLAUDE.md files

CLAUDE.md files give Claude persistent instructions for a project: build commands, test commands, and conventions.

### Set up a project CLAUDE.md

Run `/init` to generate a starting CLAUDE.md. List the commands you run most, like `./mvnw verify`.
